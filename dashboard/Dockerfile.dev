# Use an official Go runtime as a parent image
FROM golang:1.25-alpine

# Set the working directory inside the container
WORKDIR /app

# Install air for live-reloading and gcc for CGO (required by sqlite3)
RUN apk add --no-cache git gcc musl-dev && \
    go install github.com/air-verse/air@latest

# Copy the Go module files to the container
COPY go.mod go.sum ./

# Download Go module dependencies. This is cached by Docker, so it only runs
# when go.mod or go.sum changes.
RUN go mod download

# Copy the rest of the application source code
COPY . .

# Command to run the application using air for live-reloading
CMD ["air"]